# Frontend environment variables (all optional)
#
# The frontend talks to the FastAPI backend for data, auth, and the voice
# WebSocket. By default it targets the local dev server below; override this
# when you deploy the backend somewhere else. The WebSocket URL is derived
# automatically (http -> ws, https -> wss).
VITE_API_BASE_URL=http://localhost:8000
